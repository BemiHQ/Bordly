{
  "name": "bordly",
  "scripts": {
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check --write",
    "dev": "concurrently -n backend,frontend,up-fetch-emails -c green,cyan,yellow 'make up-backend' 'make up-frontend' 'make up-fetch-emails'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.12",
    "concurrently": "^9.2.1"
  }
}
