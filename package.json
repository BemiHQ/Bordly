{
  "scripts": {
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check --write",
    "dev": "concurrently -n backend,frontend -c blue,magenta 'cd backend && pnpm run dev' 'cd frontend && pnpm run dev'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.12",
    "concurrently": "^9.2.1"
  }
}
