{
  "name": "bordly",
  "scripts": {
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check --write",
    "dev": "concurrently -n backend,frontend -c blue,magenta 'make up-backend' 'make up-frontend'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.12",
    "concurrently": "^9.2.1"
  }
}
